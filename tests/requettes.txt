SELECT type,pdf,audio,video,url_Doc,img FROM resource WHERE pdf like '%' OR audio LIKE '%' OR audio LIKE '%' OR video LIKE '%' OR url_doc LIKE '%'
SELECT idcadic,person,type from resource WHERE person LIKE '%'
SELECT idcadic, title,type,pdf,audio,video,url_Doc,img FROM resource WHERE pdf like '%' OR audio LIKE '%' OR audio LIKE '%' OR video LIKE '%' OR url_doc LIKE '%'
SELECT idcadic, title,type,pdf,audio,video,url_Doc,img FROM resource WHERE idcadic is NULL
SELECT `type`, `title`, `lang`, `comment`, `person`, `oeuvre`, `organisme`, `geo`, `tag`, `analyse`, `rights`, `created_at`, `oai`, `auteur`, `resp1`, `editeur`, `editeurlieu`, `anneedit`, `isbn`, `pagination`, `collection`, `idcadic`, `updated_at`, `auteur_s`, `auteur_m`, `annee_s`, `pe_histo`, `orig_doc`, `copy_r`, `rights_a`, `support`, `couleur`, `format`, `form_file`, `duree`, `nb_files`, `cote`, `sup_num`, `loca_supnum`, `cote_num`, `loca_sup`, `img`, `pdf`, `audio`, `video`, `url_doc`, `res_edit` FROM resource WHERE type LIKE '-' OR title LIKE '-' OR lang LIKE '-' OR comment LIKE '-' OR person LIKE '-' OR oeuvre LIKE '-' OR organisme LIKE '-' OR geo LIKE '-' OR tag LIKE '-' OR analyse LIKE '-' OR rights LIKE '-' OR oai LIKE '-' OR auteur LIKE '-' OR resp1 LIKE '-' OR editeur LIKE '-' OR editeurlieu LIKE '-' OR anneedit LIKE '-' OR isbn LIKE '-' OR pagination LIKE '-' OR collection LIKE '-' OR idcadic LIKE '-' OR auteur_s LIKE '-' OR auteur_m LIKE '-' OR pe_histo LIKE '-' OR orig_doc LIKE '-' OR copy_r LIKE '-' OR rights_a LIKE '-' OR support LIKE '-' OR couleur LIKE '-' OR format LIKE '-' OR form_file LIKE '-' OR duree LIKE '-' OR nb_files LIKE '-' OR cote LIKE '-' OR sup_num LIKE '-' OR loca_supnum LIKE '-' OR cote_num LIKE '-' OR loca_sup LIKE '-' OR img LIKE '-' OR pdf LIKE '-' OR audio LIKE '-' OR video LIKE '-' OR url_doc LIKE '-' OR res_edit LIKE '-'
UPDATE `resource` SET `type` = '', `title` = '', `lang` = '', `comment` = '', `person` = '', `oeuvre` = '', `organisme` = '', `geo` = '', `tag` = '', `auteur` = '', `resp1` = '', `editeur` = '', `editeurlieu` = '', `anneedit` = '', `isbn` = '', `pagination` = '', `collection` = '', `idcadic` = '', `auteur_s` = '', `auteur_m` = '', `pe_histo` = '', `orig_doc` = '', `copy_r` = '', `support` = '', `couleur` = '', `format` = '', `form_file` = '', `duree` = '', `nb_files` = '', `cote` = '', `sup_num` = '', `loca_supnum` = '', `cote_num` = '', `loca_sup` = '', `img` = '', `pdf` = '', `audio` = '', `video` = '', `url_doc` = '', `res_edit` = '' WHERE type LIKE '-' OR title LIKE '-' OR lang LIKE '-' OR comment LIKE '-' OR person LIKE '-' OR oeuvre LIKE '-' OR organisme LIKE '-' OR geo LIKE '-' OR tag LIKE '-' OR analyse LIKE '-' OR rights LIKE '-' OR oai LIKE '-' OR auteur LIKE '-' OR resp1 LIKE '-' OR editeur LIKE '-' OR editeurlieu LIKE '-' OR anneedit LIKE '-' OR isbn LIKE '-' OR pagination LIKE '-' OR collection LIKE '-' OR idcadic LIKE '-' OR auteur_s LIKE '-' OR auteur_m LIKE '-' OR pe_histo LIKE '-' OR orig_doc LIKE '-' OR copy_r LIKE '-' OR rights_a LIKE '-' OR support LIKE '-' OR couleur LIKE '-' OR format LIKE '-' OR form_file LIKE '-' OR duree LIKE '-' OR nb_files LIKE '-' OR cote LIKE '-' OR sup_num LIKE '-' OR loca_supnum LIKE '-' OR cote_num LIKE '-' OR loca_sup LIKE '-' OR img LIKE '-' OR pdf LIKE '-' OR audio LIKE '-' OR video LIKE '-' OR url_doc LIKE '-' OR res_edit LIKE '-'
**** CADIC ****
SELECT DOC_TYPE,DOC_TITRE,DOC_AUTEUR,CND_TIT_F,DOC_LANGUE,DOC_COMMENT,CND_OEUVRE,DOC_AUTEURSEC,DOC_AUTMORAL,PHO_RESP_EDIT,DOC_DP,DOC_DP_STAT,DOC_DEE,PHO_PERSON,DOC_GEO,PHO_HISTORI,CND_COLL_AU,DOC_COLLECTION,PHO_ORIGINE,PHO_COPYRIGHT,PHO_DROIT_IMG,DOC_SUP,PHO_COULEUR,PHO_FORMAT,PHO_FORMFILE,CND_DUREE,PHO_NB_DOC,DOC_COTE,PHO_NUMCD,PHO_LOC_CD,CND_COTE_P,PHO_LOCDIAORI,AUD_REF,CND_VIDEO_REF1,CND_URL,DOC_ATTACHE,IMG_REF,DOC_REF FROM PHO_DOC WHERE DOC_ANALYSE=1 AND DOC_TYPE LIKE 'Photographie'
